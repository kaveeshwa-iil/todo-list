import React from 'react';

const Header = () => {
    return (
        <header>
           <center> <h1 className="todo-header">Todo List</h1></center>
        </header>
    );
};

export default Header;